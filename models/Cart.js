class Cart {
    constructor(id, img, name, price, type, quantity) {
        this.id = id,
            this.img = img,
            this.name = name,
            this.price = price,
            this.type = type,
            this.quantity = quantity
    }
}